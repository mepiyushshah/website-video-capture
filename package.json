{
  "name": "website-video-capture",
  "version": "1.0.0",
  "description": "Capture website videos with smooth scrolling using Playwright",
  "main": "capture.js",
  "scripts": {
    "start": "node server.js",
    "serve": "python3 -m http.server 3000",
    "capture": "node capture.js",
    "postinstall": "node -e \"if (process.env.VERCEL) { console.log('Skipping Playwright install on Vercel') } else { require('child_process').execSync('npx playwright install chromium') }\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.75.0",
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^12.4.1",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "express-session": "^1.18.2",
    "playwright": "^1.40.0",
    "playwright-core": "^1.40.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
